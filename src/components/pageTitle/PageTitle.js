import React from 'react';

export const PageTitle = ({title}) => {
    return (
        <div className='my-4 text-base  md:text-[22px] text-black font-medium'>
            {title}
        </div>
    );
};
